@model IEnumerable<Chatter.Models.Like>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Chat.MessageText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserInfo.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Chat.MessageText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserInfo.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LikeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LikeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LikeID })
        </td>
    </tr>
}

</table>
